<div class="panel panel-info">
    <div class="panel-heading">
        <h3 class="panel-title"><b>{{data.username}}</b>`s profile</h3>
    </div>
    <div class="panel-body">
        <div class="row">
            <div class="col-md-3 col-lg-3 " align="center">
                <img alt="User Pic" src="{{data.avatar}}" class="img-circle img-responsive">
            </div>
            <div class=" col-md-9 col-lg-9 ">
                <table class="table table-user-information">
                    <tbody>
                    <tr>
                        <td>Username</td>
                        <td>{{data.username}}</td>
                    </tr>
                    <tr>
                        <td>Points</td>
                        <td>{{data.points}}</td>
                    </tr>
                    {{#if data.email}}
                        <tr>
                            <td>Email</td>
                            <td><a href="mailto:info@support.com">{{data.email}}</a></td>
                        </tr>
                    {{/if}}
                    {{#if data.fName}}
                        <tr>
                            <td>First Name</td>
                            <td>{{data.fName}}</td>
                        </tr>
                    {{/if}}
                    {{#if data.lName}}
                        <tr>
                            <td>Last Name</td>
                            <td>{{data.lName}}</td>
                        </tr>
                    {{/if}}
                    {{#if data.phone}}
                        <tr>
                            <td>Phone Number</td>
                            <td>{{data.phone}}</td>
                        </tr>
                    {{/if}}
                    </tbody>
                </table>
                <a href="#/user/ads/{{data.username}}" class="btn btn-primary">My Sales Performance</a>
            </div>
        </div>
    </div>
    {{#if isOwner}}
        <div class="panel-footer">
            <a href="#/user/messages" stype="button" class="btn btn-sm btn-primary"><i
                    class="glyphicon glyphicon-envelope"></i> Messages</a>
            <span class="pull-right">
                <a href="#/user/edit/{{loggedUsername}}" type="button" class="btn btn-sm btn-warning">
                    <i class="glyphicon glyphicon-edit"></i> Edit profile
                </a>
            </span>
        </div>
    {{else}}
        <div class="panel-footer">
            <a href="#/message/send/{{data.username}}" class="btn btn-sm btn-success">
                <i class="glyphicon glyphicon-envelope"></i> Send Message
            </a>
        </div>
    {{/if}}
</div>